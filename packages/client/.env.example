VITE_SERVER_URL=http://localhost:3001

# The VAPID public key printed when generating the keypair on the server side (see
# packages/server/.env.example). Not secret — safe to expose in the client bundle.
VITE_VAPID_PUBLIC_KEY=
